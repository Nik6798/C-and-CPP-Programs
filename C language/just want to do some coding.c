main()
{
    /*pointer--> so basically pointer are the special type of variable that store the memory address(reference) of a variable
    pointer are used so that we can store the address*/
int *p,po,or;

}
